# LectureLive 安全审计情况与加固执行计划

> 文档状态：35 项代码整改与本地回归完成，待预发布/生产部署验证
> 审计日期：2026-08-20
> 当前代码基线：`91f8e40ccafe587a21e814822f0c20ceb3630f81` + 2026-08-22 安全整改工作树
> 初始全库扫描编号：`00d9fee9-43a2-4f83-a13b-f59bfb3ae9f0`
> 云端差异复核扫描编号：`02240443-18f5-414e-8a5d-ffa773f6fd6f`
> 当前修复状态：`SEC-001`～`SEC-035` **35 项代码整改完成**；全量测试、类型检查、lint、Prisma 校验与生产构建通过；生产数据迁移、Stripe 历史扫描与预发布压测尚待部署阶段执行

## 1. 总体情况

初始审计对认证与会话、权限边界、直播分享、文件与录音、LLM 调用、出站请求、配额账本、支付退款及管理员接口进行了单次标准静态安全审计。随后将本地代码从 `585444b` 快进同步至云端 `91f8e40`，并对两个新增提交执行了安全差异复核。

初始共确认 **34 项问题**；同步后新增 `SEC-035`，当前有效整改台账共 **35 项**：

| 严重度 | 数量 | 当前状态 |
| --- | ---: | --- |
| 高 | 14 | 代码整改完成，待预发布验证 |
| 中 | 16 | 代码整改完成，待预发布验证 |
| 低 | 5 | 代码整改完成，待预发布验证 |
| 严重 | 0 | 无 |

### 1.1 云端同步结果

| 顺序 | 提交 | 主要内容 | 对原 34 项的影响 |
| --- | --- | --- | --- |
| 1 | `cd080a418474c9afe8aca38ba81c2ae814827662` | 修复文档翻译上传的 `pdf-parse` 打包问题及 PDF 拖拽时误显示转录遮罩 | 0 项；属于功能和构建修复 |
| 2 | `91f8e40ccafe587a21e814822f0c20ceb3630f81` | 修复此前合并台账中的 4 个高风险问题 | `SEC-018` 部分修复；其余 33 项未触及 |

同步方式为无冲突 fast-forward；当前 `main` 与 `origin/main` 对齐。第二个提交确实封住了旧台账中的 WebM 伪造分段内存耗尽、PPTX 未闭合标签正则 CPU 放大、客户端直接修改录音时长，以及部分零时长计费路径。不过“旧台账修复 4 项”不等于本文件初始 34 项已修 4 项，两轮审计的编号和问题口径不同。

其中 PPTX 正则修复没有解决本台账 `SEC-017` 的 OOXML ZIP 条目数、压缩比和展开量缺少预检问题，因此 `SEC-017` 仍为待修复。

### 1.2 原 34 项修复复核结论

| 结论 | 数量 | 对应编号/说明 |
| --- | ---: | --- |
| 完整修复 | 0 | 暂无 |
| 部分修复 | 1 | `SEC-018` |
| 未触及 | 33 | 除 `SEC-018` 外的原问题 |
| 差异新增 | 1 | `SEC-035`：完整版转录在任务认领前整包加载录音，可被并发放大 |

`SEC-018` 只能标记为部分修复：客户端已不能通过会话 PATCH 直接写入 `durationMs`，直接音频上传也会采用服务端探测值，完整版转录会修正零时长记录；但草稿收尾仅在派生时长小于等于零时探测媒体。攻击者仍可先上传长录音，再制造一个很小但大于零的服务端会话时长，使收尾和后续计费跳过真实媒体探测，以极短时长结算长录音。

同步后的验证结果：`npm run type-check` 通过；8 个相关测试文件共 75 项测试通过。现有测试未覆盖 `SEC-018` 的“小正时长”绕过，也未覆盖 `SEC-035` 的任务认领前并发加载场景。

当前最值得优先处理的不是传统 SQL 注入或路径穿越，而是以下五类系统性问题：

1. **首次初始化边界不够严**：特定配置下，未初始化实例可能被匿名请求抢占管理员。
2. **资源和费用缺少统一预算**：直播快照、转录、附件、录音和 LLM 调用均存在不同程度的内存、存储、数据库或模型费用放大。
3. **配额依据部分来自客户端**：录音时长、附件选择及部分异步处理流程未完全以服务端测量结果结算。
4. **支付逆向链路不完整**：退款、拒付、权益回收和并发退款之间存在关联缺失或竞态。
5. **长连接与令牌撤权不彻底**：刷新令牌轮换和主持人 WebSocket 都存在旧能力继续生效的窗口。

在完成第一阶段收口前，不建议把匿名初始化、可购买 `ADMIN` 档位、生产 Stripe 测试模式或未隔离的音频增强 Worker 直接暴露在公开生产环境。

## 2. 优先级定义

| 优先级 | 对应严重度 | 处理要求 |
| --- | --- | --- |
| P1 | 高 | 第一批处理；可能导致管理员接管、全站拒绝服务、明显费用损失或支付权益损失 |
| P2 | 中 | P1 稳定后处理；主要涉及竞态、撤权窗口、局部拒绝服务和账本偏差 |
| P3 | 低 | 随后纳入常规加固；主要涉及审计、故障降级和运维面防御 |

截至 2026-08-22，35 项均已完成代码整改与本地验证，当前统一状态为 **代码修复完成（待预发布验证）**。状态流转保留为：

`待修复` → `处理中` → `待验证` → `已完成`；已有不完整修复使用 `部分修复` 标记，直到残余路径关闭并通过回归测试。

## 3. 完整问题台账

### 3.1 P1：高风险问题

| 编号 | 子系统 | 问题 | 主要代码位置 | 可能结果 | 计划处理方向 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| SEC-001 | 初始化 | 未初始化实例允许匿名抢占管理员并预写服务配置 | `src/app/api/setup/route.ts:183` | 首位管理员被外部请求抢占 | 强制一次性引导令牌；匿名阶段只允许原子认领首位管理员；完成后永久关闭引导入口 | 代码修复完成（待预发布验证） |
| SEC-002 | 代理与限流 | 默认代理信任设置可能使 HTTP/WS 客户端身份退化为共享桶 | `.env.example:76` | 限流误伤、绕过或连接上限整体失真 | 明确可信代理跳数；启动时校验部署配置；Nginx 增加独立 IP 和全局限流 | 代码修复完成（待预发布验证） |
| SEC-003 | 直播分享 | 观众可重复加入，放大数据库查询、快照发送和房间扫描 | `src/lib/liveShare/server.ts:846` | 数据库和 WebSocket 带宽被低成本消耗 | 加入操作幂等化；缓存短期授权；限制连接级加入频率和响应总字节 | 代码修复完成（待预发布验证） |
| SEC-004 | 直播分享 | 快照按元素数而非聚合字节控制，可累积巨大常驻对象 | `src/lib/liveShare/server.ts:132` | WebSocket 进程内存耗尽并影响全站直播 | 使用严格 schema 丢弃未知字段；维护会话、用户和全局快照字节预算 | 代码修复完成（待预发布验证） |
| SEC-009 | 转录存储 | 会话定稿和转录接口接受无总量上限的转录包 | `src/lib/sessionApi.ts:92` | JSON、数据库、磁盘和后续处理资源被放大 | 限制段数、字段长度、嵌套深度和序列化总字节；写入前计入存储账本 | 代码修复完成（待预发布验证） |
| SEC-010 | LLM 报告 | 报告接口可重复触发大量未计量模型调用 | `src/app/api/llm/report/route.ts:92` | 平台模型费用和任务队列被放大 | 按会话版本单飞去重；复用结果；调用前原子预留 token/费用预算 | 代码修复完成（待预发布验证） |
| SEC-011 | 关键词提取 | 无界 `existingKeywords` 被复制进最多数十次模型提示 | `src/app/api/llm/extract-keywords/route.ts:153` | 单请求被放大为多次大上下文付费调用 | 限制字段字节、关键词数量和总 token；分块调用共享固定摘要而非复制全文 | 代码修复完成（待预发布验证） |
| SEC-012 | LLM/RAG | 单个超长转录段可越过总体字符上限并制造无界 embedding 批次 | `src/app/api/llm/chat/route.ts:157` | 内存、嵌入费用和上游调用数量失控 | 先按单段和总 token 双重截断；限制批次数量与并发；调用前预留预算 | 代码修复完成（待预发布验证） |
| SEC-013 | 全局对话 | 对话可挂载无界录音集合并一次性加载、嵌入全部内容 | `src/app/api/conversations/route.ts:220` | 一个请求加载大量录音并产生高额嵌入费用 | 限制录音数量、总时长和总字节；分页选择；增量索引而非请求时全量嵌入 | 代码修复完成（待预发布验证） |
| SEC-015 | 聊天附件 | `attachmentIds` 为空时会在裁剪前并发下载全部附件 | `src/lib/llm/chatAttachments.ts:174` | 数百 MB 至 GB 级内存峰值和进程崩溃 | 空选择表示不加载；下载前按元数据限制数量/总字节；采用有界并发和流式读取 | 代码修复完成（待预发布验证） |
| SEC-016 | 聊天图片 | 内联图片绕过存储配额并在模型调用前永久落盘 | `src/lib/llm/chatImageStorage.ts:64` | 活动对话可持续填满本地磁盘 | 统一建模为附件；写前原子预留；失败回滚；增加累计上限、TTL 和孤儿清理 | 代码修复完成（待预发布验证） |
| SEC-018 | 录音计费 | 客户端 PATCH 与直接上传路径已收口，但草稿收尾仍信任很小的正 `serverDurationMs` 并跳过媒体探测 | `src/app/api/sessions/[id]/audio/draft/finalize/route.ts:97` | 低配额账户仍可把长录音按极短时长保存、转录和计费 | 所有发布/收尾路径都以服务端媒体探测值结算；探测失败关闭失败；已结算时长不可由客户端覆盖 | 代码修复完成（待预发布验证） |
| SEC-025 | 支付拒付 | 充值余额消费后再拒付，不会完整追缴下游会员等权益 | `src/lib/payment/refundHandling.ts:137` | 用户在拒付后继续保留已兑换权益 | 权益绑定资金来源批次；支持负债、冻结和权益回收；异常进入人工审核 | 代码修复完成（待预发布验证） |
| SEC-026 | Stripe Webhook | 退款和争议通常无法关联本地订单，却可能被成功确认 | `src/lib/payment/refundHandling.ts:63` | 拒付永久漏处理，资金和权益账不一致 | 保存 PaymentIntent/Charge 映射；未知事件持久化待处理并告警；可靠重试而非静默 ACK | 代码修复完成（待预发布验证） |

### 3.2 P2：中风险问题

| 编号 | 子系统 | 问题 | 主要代码位置 | 可能结果 | 计划处理方向 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| SEC-005 | 刷新令牌 | 已轮换的旧 JWT 在宽限窗内可换回完整后继 JWT | `src/app/api/auth/refresh/route.ts:90` | 被窃取旧令牌可继续取得当前会话 | 实现一次性 token family；检测重用后撤销整族；不缓存完整后继 JWT | 代码修复完成（待预发布验证） |
| SEC-006 | 刷新令牌 | 并发刷新可从同一旧 JWT 产生多个有效兄弟会话 | `src/app/api/auth/refresh/route.ts:71` | 单次令牌轮换变成多个并行有效会话 | 通过数据库 CAS 或事务保证旧凭据只消费一次 | 代码修复完成（待预发布验证） |
| SEC-007 | 公共认证 | 登录等接口在 IP 限流前解析正文并创建高基数账户桶 | `src/app/api/auth/login/route.ts:20` | 匿名请求放大正文解析、哈希和限流状态 | 先执行有界 IP/全局门禁；限制请求体；账户桶采用定长哈希与有界缓存 | 代码修复完成（待预发布验证） |
| SEC-014 | 录音关联 | 无界 ID 数组直接进入 Prisma 批量查询和写入 | `src/app/api/conversations/[id]/recordings/route.ts:90` | 超大 SQL、数据库内存和事务压力 | 限制 ID 数量与总字节；去重后分批查询和写入 | 代码修复完成（待预发布验证） |
| SEC-017 | 文档解析 | OOXML ZIP 在检查前解析全部目录，且不限制条目数和压缩比 | `src/lib/fileParser.ts:85` | 恶意文档造成 CPU/堆耗尽 | 解析前预检中央目录；限制条目、路径、压缩比和展开量；移入受限子进程 | 代码修复完成（待预发布验证） |
| SEC-019 | 存储对账 | 每日字节对账可能把已计费录音字节重置为零 | `src/lib/quota.ts:826` | 用户恢复虚假可用配额并继续写入 | 所有 artifact 统一记录实际字节；对账只根据完整账本重建 | 代码修复完成（待预发布验证） |
| SEC-020 | 音频 Worker | 用户文件进入未严格限制协议的 ffprobe/ffmpeg | `worker/audio-enhance-worker.mjs:293` | SSRF、本地协议访问或解析器资源攻击 | 固定协议白名单；禁用网络；服务端生成路径；子进程资源和期限限制 | 代码修复完成（待预发布验证） |
| SEC-021 | 翻译 LLM 代理 | 先调用付费模型、后计量，任务预算可被并发击穿 | `src/app/api/translate/llm-proxy/v1/chat/completions/route.ts:64` | 恶意或失陷 Worker 大幅超出任务预算 | 请求前原子预留预计 token；完成后按实际结算；计量失败关闭；限制任务并发 | 代码修复完成（待预发布验证） |
| SEC-022 | WebSocket 授权 | 主持人只在握手时鉴权，撤权后旧连接仍可广播 | `src/lib/liveShare/server.ts:563` | 登出、封禁或分享链接轮换后仍能篡改直播内容 | 周期和敏感事件前重验用户、token family、所有权及分享世代；撤权时主动断连 | 代码修复完成（待预发布验证） |
| SEC-023 | 角色权限 | 可购买充值档位允许把普通用户提升为 `ADMIN` | `src/app/api/admin/recharge/tiers/route.ts:114` | 商业购买路径变成权限提升路径 | 角色与付费权益彻底分离；数据库约束禁止商品授予管理角色 | 代码修复完成（待预发布验证） |
| SEC-024 | Stripe 配置 | 生产环境可使用 Stripe 测试凭据结算真实钱包与权益 | `src/lib/payment/providers/stripe.ts:97` | 无真实资金却获得生产权益 | 启动和 webhook 双重校验 `livemode`；生产环境拒绝 test key/event | 代码修复完成（待预发布验证） |
| SEC-027 | 钱包发放 | 退款可与分段发放竞态，使已退款订单继续授予权益 | `src/lib/wallet.ts:190` | 退款完成后仍出现余额或会员权益 | 订单状态、扣减和发放放入同一锁或条件事务 | 代码修复完成（待预发布验证） |
| SEC-028 | 会员退款 | 并发撤销多笔会员订单可能丢失一次到期日缩减 | `src/lib/payment/refundHandling.ts:118` | 用户少退一期权益 | 锁定会员记录；使用可组合的权益流水账，避免读取后覆盖写 | 代码修复完成（待预发布验证） |
| SEC-029 | 支付订单 | 订单 `expiresAt` 未阻止迟到结算 | `src/lib/wallet.ts:281` | 过期价格或失效订单仍获得权益 | 把过期时间加入原子状态转换条件；迟到支付进入退款或人工核对 | 代码修复完成（待预发布验证） |
| SEC-031 | 客户端隔离 | 401 或直接换号时未统一清理账号相关状态和私有图片缓存 | `src/components/AuthSessionMonitor.tsx:74` | 同一浏览器后续用户看到前一账号的本地数据 | 建立单一登出/主体变化清理器；清空 Zustand、查询缓存、对象 URL 和私有图片缓存 | 代码修复完成（待预发布验证） |
| SEC-035 | 完整版转录 | 零时长录音在任务原子认领前被整包读入内存并执行媒体探测，且路由缺少并发门禁 | `src/app/api/sessions/[id]/full-transcribe/route.ts:74` | 同一用户并发请求可放大数百 MB 录音的内存、临时磁盘和探测进程消耗，拖垮共享服务 | 先原子认领任务再读取文件；改用文件路径或流式、有字节上限的探测；增加用户/会话/全局并发预算 | 代码修复完成（待预发布验证） |

### 3.3 P3：低风险问题

| 编号 | 子系统 | 问题 | 主要代码位置 | 可能结果 | 计划处理方向 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| SEC-008 | JWT 撤销 | Redis 故障或淘汰时，多进程撤销检查失败开放 | `src/lib/auth.ts:211` | 被撤销令牌在部分进程短期恢复有效 | 关键撤销状态持久化；无法确认时限制权限或关闭失败；监控撤销后端健康 | 代码修复完成（待预发布验证） |
| SEC-030 | 管理员对账 | 对账修复可能覆盖在途预留并导致最终净扣不足 | `src/app/api/admin/reconciliation/fix/route.ts:96` | 并发任务消耗未被正确计入 | 运维修复前暂停相关写入，或在事务中基于完整流水重建 | 代码修复完成（待预发布验证） |
| SEC-032 | 健康检查 | 匿名健康检查把每个请求放大为多类依赖探测 | `src/app/api/health/route.ts:5` | 匿名请求持续施压数据库及 Redis | 拆分轻量存活与受限深度就绪检查；增加来源、频率和超时控制 | 代码修复完成（待预发布验证） |
| SEC-033 | 管理员审计 | 敏感读取和通用跨用户修改缺少完整审计事件 | `src/app/api/admin/share-links/route.ts:93` | 事故后无法可靠追踪管理员行为 | 记录操作者、目标、前后值、原因、请求 ID 和时间，并保护日志完整性 | 代码修复完成（待预发布验证） |
| SEC-034 | LLM 出站 | 修改服务端点时可复用不可见旧凭据 | `src/app/api/admin/llm-providers/[id]/route.ts:46` | 旧密钥被发送至管理员新指定的主机 | 端点或提供商变更时强制重新输入密钥；加入允许域和重认证 | 代码修复完成（待预发布验证） |

## 4. 分阶段执行方案

### 阶段 A：紧急收口（预计 0.5–1 天）

目标：先封住管理员接管、配置误用和最容易产生资金损失的入口。

- [x] 修复 SEC-001：初始化必须提供一次性引导令牌，并限制为首次管理员认领。
- [x] 修复 SEC-002：明确 Nginx、Next.js 和 Socket.IO 的真实 IP 信任链。
- [x] 修复 SEC-023：商品及充值档位禁止授予 `ADMIN`。
- [x] 修复 SEC-024：生产环境强制 Stripe live 模式。
- [x] 收紧报告生成、附件全量加载、内联图片和音频增强入口。
- [ ] 检查历史 Stripe refund/dispute 是否存在未关联或未追缴事件。

验收条件：

- 无引导令牌的匿名请求不能创建管理员或写入任何服务配置。
- 代理链测试可以稳定区分两个真实客户端，伪造转发头不会改变身份。
- 数据库和 API 均无法创建可购买的管理员档位。
- 生产配置加载 test key 或 test webhook 时启动/处理失败并告警。

### 阶段 B：身份、令牌与实时连接（预计 1–3 天）

目标：建立可撤销、一次性、可重新验证的能力边界。

- [x] 修复 SEC-005、SEC-006：实现原子 refresh-token family 和重用检测。
- [x] 修复 SEC-003：直播加入幂等化并增加成本型限流。
- [x] 修复 SEC-004：快照使用严格 schema 和聚合字节预算。
- [x] 修复 SEC-022：主持人长连接支持撤权重验和主动断连。
- [x] 修复 SEC-007、SEC-008：公共认证前置门禁和持久撤销策略。
- [x] 修复 SEC-031：账号主体变化时统一清理客户端私有状态。

验收条件：

- 同一旧 refresh token 并发请求最多只有一个成功。
- 已消费 refresh token 再次出现时撤销整个 token family。
- 封禁、登出、分享链接轮换后，旧主持人连接无法继续广播。
- 大量重复加入不会线性增加数据库查询和快照发送。
- 快照达到字节预算后被拒绝并产生可观测事件，进程内存保持稳定。

### 阶段 C：资源、存储和 LLM 成本治理（预计 3–7 天）

目标：所有攻击者可控资源都在分配或付费调用前获得硬预算。

- [x] 修复 SEC-009、SEC-012：转录段数、单段、总字节、总 token 和 embedding 批次上限。
- [x] 修复 SEC-010、SEC-011、SEC-021：LLM 单飞、调用前原子预留和实际用量结算。
- [x] 修复 SEC-013、SEC-014、SEC-015：录音/附件数量、字节、时长和数据库批次上限。
- [x] 修复 SEC-016：内联图片纳入附件和存储账本。
- [x] 修复 SEC-018、SEC-019：服务端测量音频时长，建立统一 artifact 字节账本。
- [x] 修复 SEC-035：任务认领前不加载录音，媒体探测采用有界流/路径并受分层并发预算保护。

建议统一实现一个资源准入层，至少覆盖：

- 请求体实际读取字节；
- 用户持久存储字节；
- 活跃上传和处理中临时字节；
- 录音总时长；
- LLM 输入、输出及 embedding token；
- 每用户、每任务和全局并发；
- 失败、取消和超时后的额度释放。

验收条件：

- 所有限额由服务端测量值决定，客户端声明只作为提示。
- 并发请求不能共同越过同一余额或 token 预算。
- 所有持久文件均可从数据库账本追溯，并能通过对账恢复相同结果。
- 模型或数据库失败后，不遗留文件、预留额度或无引用图片。

### 阶段 D：支付、退款与权益一致性（预计 3–5 天）

目标：把资金事件、余额、会员和订单状态变成可追溯且可逆的事务链。

- [x] 修复 SEC-025、SEC-026：记录 Stripe 对象映射，拒付后追踪并回收下游权益。
- [x] 修复 SEC-027、SEC-028：发放与退款加锁或条件事务化。
- [x] 修复 SEC-029：执行订单过期状态机。
- [x] 为未知、乱序和重复 webhook 建立幂等收件箱与人工处理队列。
- [x] 增加资金来源批次、负债、冻结和人工解除能力。

验收条件：

- 同一 webhook 重放任意次数都只产生一次业务效果。
- 退款和发放并发时，最终状态只能是完整发放或完整退款之一。
- 充值余额被兑换后拒付，系统仍能准确定位并处理下游权益。
- 未映射、乱序或处理失败事件不会被永久丢弃。

### 阶段 E：文件解析、出站与运维面（预计 2–4 天）

目标：隔离复杂解析器和高权限运维操作的爆炸半径。

- [x] 修复 SEC-017：Office/PDF 解析移到受限、可终止的 Worker。
- [x] 修复 SEC-020：ffmpeg/ffprobe 禁止网络及危险协议，并设置 OS 级资源限制。
- [x] 修复 SEC-030、SEC-032、SEC-033、SEC-034：安全对账、健康检查分层、管理员审计和端点变更重认证。

验收条件：

- ZIP 条目爆炸、高压缩比、超多 XML 节点和解析超时均被隔离进程终止。
- 媒体处理器不能访问 HTTP、内网地址、本地任意协议或非任务文件。
- 管理员敏感操作具备完整审计轨迹。
- 深度健康检查不能由匿名互联网请求高频触发。

## 5. 验证与回归要求

每一批修复完成后至少执行：

```bash
npm run type-check
npm run lint
npm test
```

此外需要补充以下专项测试：

| 测试组 | 必测场景 |
| --- | --- |
| 初始化与代理 | 并发抢占首位管理员、无 token、错误 token、伪造 `X-Forwarded-For`、多级代理 |
| 刷新令牌 | 双并发刷新、旧 token 重放、Redis 故障、封禁和密码修改后的撤销 |
| WebSocket | 重复加入、超大快照、未知嵌套字段、主持人撤权、链接轮换、断线重连 |
| LLM 与附件 | 超长单段、超多分块、空附件列表、大附件集合、模型超时、计量写入失败 |
| 录音与存储 | 零时长和小正时长伪造、并发上传、覆盖录音、对账、失败回滚、存储后端异常 |
| 完整版转录 | 大录音零时长、同会话并发、认领前不读取文件、流式探测上限、探测失败关闭失败 |
| 文件解析 | 海量 ZIP entry、高压缩比、超多 XML 节点、超时、Worker OOM 和取消 |
| 支付 | 重复/乱序 webhook、迟到支付、退款与发放竞态、多笔并发退款、拒付后权益回收 |
| 客户端隔离 | 401、主动登出、直接换号、浏览器前进后退、私有图片和对象 URL 缓存 |

生产观测至少增加以下指标和告警：

- 初始化失败和重复尝试；
- 每 IP、用户、会话的 HTTP/WS 拒绝数；
- WebSocket 快照字节与房间连接数；
- 上传临时字节、永久 artifact 字节和配额预留；
- LLM 请求 token、预计费用、实际费用和预算拒绝；
- 文档/媒体 Worker 队列、CPU、内存、超时和异常退出；
- Stripe 未映射事件、处理重试、拒付金额和权益回收失败；
- 管理员高敏操作审计失败。

## 6. 本轮范围限制

本轮结论来自源码静态追踪和交叉验证，没有对生产环境执行破坏性利用。以下工作仍未覆盖：

- 第三方依赖 SCA、许可证与供应链漏洞扫描；
- 真实部署下的并发压力、故障注入和容量基线；
- DNS 重绑定、逐跳重定向及实际网络 peer 校验；
- 各 LLM、存储和支付供应商异常响应的完整边界测试；
- 云端、Nginx、Redis、MySQL 和对象存储的实际生产配置核查。

本次已对 `585444b..91f8e40` 执行安全差异复审，但没有做动态并发压测。因此，完成后续代码整改后仍需再次复审对应 diff，并在预发布环境执行压力与故障测试。

## 7. 建议的第一批提交拆分

为降低一次改动过大带来的回归风险，建议按以下顺序拆分提交：

1. `security: 收紧首次初始化与代理身份边界`
2. `security: 禁止商品授予管理员并锁定 Stripe 生产模式`
3. `security: 关闭录音小正时长绕过并前置转录任务认领`
4. `security: 为报告、关键词、附件和图片增加硬预算`
5. `security: 完整关联 Stripe 退款拒付与下游权益`
6. `security: 原子化刷新令牌并支持 WebSocket 撤权`
7. `security: 隔离 Office 与媒体解析 Worker`

第一批完成并通过验证后，再处理其余并发一致性、运维审计和低风险加固项。

## 8. 执行记录

| 日期 | 批次 | 涉及编号 | 状态 | 验证结果 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 2026-08-20 | 审计与计划 | SEC-001～SEC-034 | 已完成 | 静态审计报告已封存 | 尚未修改业务代码 |
| 2026-08-20 | 云端同步与差异复核 | SEC-018、SEC-035 | 已完成 | 类型检查通过；相关 8 个测试文件、75 项测试通过 | 原 34 项：0 项完整修复、1 项部分修复、33 项未触及；新增 1 项中风险 |
| 2026-08-22 | 分批代码整改与单人收尾 | SEC-001～SEC-035 | 代码修复完成 | `tsc --noEmit`、lint、Prisma schema、Shell 语法、WebSocket bundle、Next.js 生产构建通过；325 个测试文件、2775 项测试通过，1 项按配置跳过 | 生产数据迁移/回填、Stripe 全历史扫描、真实依赖故障注入与容量压测待部署阶段执行 |

## 9. 部署前剩余操作

以下不是遗留源码漏洞，而是需要真实环境或生产数据的上线动作：

- [ ] 在维护窗口执行 Prisma 迁移、StoredArtifact 回填和安全约束预检，完成后再恢复付费写入。
- [ ] 配置独立强 `JWT_SECRET` / `ENCRYPTION_KEY`、明确代理链、LLM 出站 allowlist 与 Stripe live 凭据，再运行 readiness 检查。
- [ ] 运行 Stripe 全历史 refund/dispute 对账，对未映射、乱序或追缴异常事件进入人工复核。
- [ ] 在预发布环境执行真实 MySQL/Redis/对象存储/支付供应商的并发压测、故障注入和容量基线。
